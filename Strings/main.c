#include <stdio.h>
#include "functions.h"


int main()
{
    char string[] = "finding";

    // duplicateStringBitwise(string);

    // anagram("deecimmmal", "medimcale");

    permutation("abc", 0);
}